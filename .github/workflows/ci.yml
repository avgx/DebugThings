name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build and test (macOS, Swift 6.2)
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Swift 6.1.3 can abort in GenericSpecializer/JVPCloner when optimizing Pulse
      # LoggerStore.initializeViewContext; 6.2.x toolchains compile this project cleanly.
      - name: Select Swift 6.2
        uses: swift-actions/setup-swift@v3
        with:
          swift-version: "6.2"

      - name: Swift version
        run: swift --version

      - name: Build (debug)
        run: swift build -v

      - name: Run tests
        run: swift test -v
